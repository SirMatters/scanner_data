presets:
- filter:
  - left: name
    operation: match
    right: USD$
  - left: sector
    operation: nempty
  - left: market_cap_calc
    operation: nempty
  name: market-cap-rank
  sort:
    sortBy: market_cap_calc
    sortOrder: desc
- filter:
  - left: typespecs
    operation: has
    right: defi
  - left: name
    operation: match
    right: USD$
  - left: sector
    operation: nempty
  - left: market_cap_calc
    operation: nempty
  name: top-defi
  sort:
    sortBy: market_cap_calc
    sortOrder: desc
